---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

type Props = {
    src: ImageMetadata;
    alt?: string;
};

const { src, alt = "" } = Astro.props;
---

<Image src={src} alt={alt} class="my-4 w-full" />
